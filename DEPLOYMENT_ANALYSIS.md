# Анализ возможности развертывания на icesmoke.store/voice/

## Структура сервера (по скриншоту):

```
/
├── .cache, .cagefs, .config, .local, .ssh - системные папки
├── bin-tmp, email, logs, mod-tmp, php-bin - служебные папки
├── www/ - основная папка для веб-контента
├── tmp/ - временные файлы
└── инструкция.txt - инструкции хостинга
```

## ✅ Возможности развертывания:

### Вариант 1: Если есть доступ к Nginx/Apache конфигурации

**Можно развернуть на `/voice/`:**

1. **Загрузить код** в папку (например, `www/voice/` или отдельную папку)
2. **Настроить веб-сервер** для проксирования `/voice/` на Flask (порт 5000)
3. **Запустить Flask** через Gunicorn на `127.0.0.1:5000`

**Требования:**
- Доступ к конфигурации Nginx/Apache
- Возможность запускать Python приложения
- Возможность устанавливать Python пакеты

### Вариант 2: Если НЕТ доступа к конфигурации веб-сервера (shared hosting)

**Ограничения:**
- Нет доступа к Nginx/Apache конфигурации
- Нельзя настроить проксирование на локальный порт
- Обычно только PHP поддерживается

**Альтернативы:**
1. **Использовать поддомен**: `voice.icesmoke.store` (если доступно)
2. **Использовать отдельную папку**: `icesmoke.store/voice/` как статический сайт (не подходит для Flask)
3. **CGI/FastCGI**: Если хостинг поддерживает Python через CGI (редко)

## 🔍 Что нужно проверить:

### 1. Тип хостинга:
- **VPS/Выделенный сервер** → ✅ Можно настроить Nginx
- **Shared hosting** → ❌ Скорее всего нельзя

### 2. Доступ к конфигурации:
- Есть ли доступ к `/etc/nginx/` или панели управления веб-сервером?
- Можно ли создавать виртуальные хосты?

### 3. Python поддержка:
- Установлен ли Python 3?
- Можно ли устанавливать пакеты через `pip`?
- Есть ли доступ к `systemd` для создания сервисов?

### 4. Структура `www/`:
- Что находится в папке `www/`?
- Есть ли там уже сайт на icesmoke.store?
- Можно ли создать подпапку `www/voice/`?

## 📋 План действий (если есть доступ к Nginx):

### Шаг 1: Загрузка кода
```bash
# Загрузить код в папку (например)
/home/u171349/www/voice/
# или
/home/u171349/order-assistant/
```

### Шаг 2: Настройка Nginx
Добавить в конфигурацию домена `icesmoke.store`:
```nginx
location /voice/ {
    rewrite ^/voice/(.*) /$1 break;
    proxy_pass http://127.0.0.1:5000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

### Шаг 3: Запуск приложения
```bash
# Через systemd (если есть доступ)
# Или через screen/tmux
screen -S order-assistant
cd /path/to/order-assistant
source venv/bin/activate
gunicorn --workers 3 --bind 127.0.0.1:5000 wsgi:app
```

## ⚠️ Важные вопросы:

1. **Есть ли доступ к SSH?** (для запуска Python приложения)
2. **Какой веб-сервер?** (Nginx, Apache, или что-то другое)
3. **Есть ли панель управления?** (ISPmanager, cPanel, и т.д.)
4. **Что уже работает на icesmoke.store?** (чтобы не сломать существующий сайт)

## 💡 Рекомендации:

1. **Сначала проверьте доступ к конфигурации веб-сервера**
2. **Если нет доступа** → рассмотрите поддомен `voice.icesmoke.store`
3. **Если есть доступ** → следуйте плану выше

## 📝 Что нужно уточнить у хостинг-провайдера:

- Тип хостинга (VPS/Shared)
- Доступ к конфигурации Nginx/Apache
- Поддержка Python приложений
- Возможность создания поддоменов

